Notes

get_block( blk, buf)
				0x9000
				es < DS amd SS   char (global) dbuf(1024)

		ox9000
_
|_code
|_data
|_bss 

so dont worry aBOUT BUFFER SIZE because its allocated later

getblk(
diskr( (2*blk)/36, ((2*blk)%36)/18, (((2*blk)%26)%18)+1, buf
)

lookup diskr syntax

use ungisned short
